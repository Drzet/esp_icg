name: ESP-IDF build

on:
  push:
    branches:
      - feature/icg-prototype
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: espressif/idf:v6.0.3

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Show toolchain
        run: |
          idf.py --version
          python --version

      - name: Set target
        run: idf.py set-target esp32p4

      - name: Build
        run: idf.py build

      - name: Upload firmware
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: esp_icg-esp32p4
          path: |
            build/*.bin
            build/*.elf
            build/*.map
            build/bootloader/*.bin
            build/partition_table/*.bin
          if-no-files-found: error
